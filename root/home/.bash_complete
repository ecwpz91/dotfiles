# .bash_complete

# ----------------------------------------------------------------------
# OC
# ----------------------------------------------------------------------
if [ -f $HOME/.local/bin/oc.sh ]; then
 source $HOME/.local/bin/oc.sh
fi

# ----------------------------------------------------------------------
# OC-Cluster
# ----------------------------------------------------------------------
if [ -f $HOME/.local/bin/oc-cluster.sh ]; then
 source $HOME/.local/bin/oc-cluster.sh
fi

# ----------------------------------------------------------------------
# Minikube
# ----------------------------------------------------------------------
if [ -f $HOME/.local/bin/minikube.sh ]; then
 source $HOME/.local/bin/minikube.sh
fi
